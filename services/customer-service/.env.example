# ------------------------------------------------------------------------------
# Environment Configuration for BillFlow Customer Service
#
# Copy this file to `.env` and fill in the values. Never commit real secrets!
# ------------------------------------------------------------------------------

# Rails runtime environment:
#  - development: local work, full logging
#  - test:         for your automated specs
#  - production:   optimized for deployment
RAILS_ENV=<RAILS_ENVIRONMENT>    # Default: development

# ------------------------------------------------------------------------------
# Database Connection (PostgreSQL)
# ------------------------------------------------------------------------------

# Hostname or Docker service name where Postgres is running
DB_HOST=<Database_Host>          # Default: customer-db

# Credentials for connecting to Postgres
DB_USERNAME=<Database_Username>  # Default: billflow
DB_PASSWORD=<Database_Password>  # Default: password

# Name of the development database
DB_NAME=<Database_Name>          # Default: billflow_customer_development

# ------------------------------------------------------------------------------
# JSON Web Token (JWT) Settings
# ------------------------------------------------------------------------------

# Secret key used to sign and verify JWT tokens.
#   • In production, use a securely generated random string.
#   • Keep this value private and rotate regularly.
JWT_SECRET=<your_secure_jwt_string>

# ------------------------------------------------------------------------------
# Test Database Configuration
# ------------------------------------------------------------------------------

# Name of the database used when running Rails in the test environment.
TEST_DB_NAME=<Test_Database_Name>  # Default: billflow_customer_test
